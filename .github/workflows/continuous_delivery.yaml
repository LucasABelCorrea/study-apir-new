name: Continuous Delivery

on:
    push:

jobs:
    push_to_docker_hub:
        runs-on: ubuntu-latest
        steps:
            -   name: Imprimir Mensagem
                run: echo "Olá, mundo!"
            -   name: Step 2
                run: |
                    echo "Nova instrução"
                    ls
            -   name: Step 3
                run: echo "Ka loteira"

